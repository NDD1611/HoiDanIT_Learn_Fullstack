<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GET CRUD</title>

    <style>
        table,
        th,
        td {
            border: 1px solid black;
        }

        .btn-edit {
            background-color: yellow;
            border: none;
            border-radius: 3px;
            width: 60px;
            height: 20px;
            cursor: pointer;
        }

        .btn-del {
            background-color: red;
            border: none;
            border-radius: 3px;
            width: 60px;
            height: 20px;
            cursor: pointer;
        }
    </style>
</head>

<body>
    <table style="width:100%">
        <tr>
            <th>Email</th>
            <th>First Name</th>
            <th>Last Name</th>
            <th>Address</th>
            <th>Actions</th>
        </tr>
        <!-- cu phap lap trong ejs -->
        <% for(let i=0; i< dataTables.length; i++) { %>
            <tr>
                <td>
                    <%= dataTables[i].email %>
                </td>
                <td>
                    <%= dataTables[i].firstName %>
                </td>
                <td>
                    <%= dataTables[i].lastName %>
                </td>
                <td>
                    <%= dataTables[i].address %>
                </td>
                <td>
                    <a href="/edit-crud?id=<%= dataTables[i].id %>" class="btn-edit" type="button">Edit</a>
                    <a href="/delete-crud?id=<%= dataTables[i].id %>" class="btn-del" type="button">Delete</a>
                </td>
            </tr>
            <% } %>
    </table>
</body>

</html>